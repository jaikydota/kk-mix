[project]
name = "han-mix"
version = "7.0.0"
description = "FFmpeg 批处理视频编辑工具"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.1.0",
    "moviepy==1.0.3",
    "numpy==2.0.2",
    "pillow==11.3.0",
]

[dependency-groups]
dev = [
    "pyinstaller==6.19.0",
]

[tool.uv]
package = false
