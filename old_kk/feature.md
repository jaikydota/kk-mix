# kk-mix 智能视频剪辑工具 — 功能文档

> **版本：** v8.1  
> **平台：** Windows  
> **技术栈：** Python + Tkinter + FFmpeg  
> **打包：** PyInstaller → `dist/kk.exe`（内嵌 ffmpeg.exe）

---

## 整体界面结构

工具采用**双行标签页**布局，共 13 个功能模块，底部统一提供：

- **并行线程数** 设置（当前固定为 1）
- **极速模式**（ultrafast 编码预设，牺牲少量画质换取速度）
- **暂停 / 停止** 控制按钮（所有批处理操作均支持）
- **进度条** + **状态栏**
- **操作日志**（带时间戳，实时滚动）
- 顶栏：**FFmpeg 路径显示**、**测试 FFmpeg**、**全局设置（MCP / LLM）**

---

## 第一行功能菜单（Row 1）

### 1. 左右分屏合并

将两个文件夹中的视频/图片**按顺序两两配对**，横向拼接为左右分屏视频。

| 参数 | 说明 |
|------|------|
| 文件夹 1 | 左侧视频/图片来源 |
| 文件夹 2 | 右侧视频/图片来源 |
| 输出文件夹 | 生成文件的保存路径 |
| 音频源 | 使用文件夹 1 的音频 / 文件夹 2 的音频 / 无音频（三选一）|
| 图片显示时长 | 当输入为图片时，每张图片在视频中的持续秒数（默认 5 秒）|

**输出：** 逐对生成 `merge_001_xxx.mp4`，左右画面等比缩放拼接。

---

### 2. 批量分割视频

按固定时长将视频文件夹中的所有视频**切成多段**。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 待分割的视频来源 |
| 每段时长（秒） | 默认 30 秒 |
| 视频输出文件夹 | 分段视频保存路径 |
| 音频输出文件夹（可选）| 若填写则同步提取每段的音频文件 |
| 保留不足时长的最后片段 | 勾选后最后一段即便不满时长也会保留 |
| 提取音频到独立文件 | 额外输出每段对应的 `.mp3` 音频 |

**输出：** `split_001_part001_xxx.mp4`，可选 `split_001_part001_xxx.mp3`。

---

### 3. 视频转场拼接

将**多个文件夹**的视频**按顺序**首尾拼接，两段之间可加入过渡转场效果。

| 参数 | 说明 |
|------|------|
| 视频文件夹（可无限添加）| 最少 1 个，默认显示 5 个输入框，可点击"＋添加文件夹"动态扩展，第 6 个起可删除 |
| 输出文件夹 | 拼接结果保存路径 |
| 转场时长（秒）| 0 表示无转场直接拼接，默认 0.5 秒 |
| 转场类型 | 支持 20 种效果：`fade`、`fadeblack`、`fadewhite`、`wipeleft/right/up/down`、`slideleft/right/up/down`、`smoothleft/right/up/down`、`circleopen`、`circleclose`、`radial`、`dissolve`、`pixelize` |

**输出：** 各文件夹同名视频配对拼接，生成 `concat_001_xxx.mp4`。

---

### 4. 画中画合成

将一个**前景视频/图片**叠加在**背景视频**上，形成画中画效果。

| 参数 | 说明 |
|------|------|
| 背景视频文件夹 | 主画面视频来源 |
| 前景视频/图片文件夹 | 叠加的小窗内容 |
| 输出文件夹 | 结果保存路径 |
| 位置 | `top_left`、`top_right`、`bottom_left`、`bottom_right` |
| 缩放比例 | 前景相对于背景的宽度占比，范围 0.1～1.0，默认 0.3 |

**输出：** `pip_001_xxx.mp4`，背景与前景按对配对处理。

---

### 5. 批量变速/倒放

对文件夹内所有视频进行**加速、减速或倒放**处理。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 待处理视频来源 |
| 输出文件夹 | 结果保存路径 |
| 速度倍数 | `0.5` = 慢放一半，`1.0` = 原速，`2.0` = 两倍速，默认 1.5 |
| 倒放视频 | 勾选后视频画面和音频均时间反转 |

**输出：** `speed_001_xxx.mp4`。音视频同步变速。

---

### 6. 批量旋转/翻转

对文件夹内所有视频进行旋转或镜像翻转。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 待处理视频来源 |
| 输出文件夹 | 结果保存路径 |
| 操作类型 | `90` / `180` / `270`（顺时针旋转）、`hflip`（水平镜像）、`vflip`（垂直翻转）|

**输出：** `rotate_001_90_xxx.mp4`（后缀包含操作类型）。

---

### 7. 批量添加水印

为文件夹内所有视频批量叠加**图片水印**。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 待处理视频来源 |
| 水印图片 | 支持 PNG / JPG / JPEG，推荐使用透明背景 PNG |
| 输出文件夹 | 结果保存路径 |
| 位置 | `top_left`、`top_right`、`bottom_left`、`bottom_right`、`center` |
| 透明度 | 范围 0.1～1.0，默认 0.8 |
| 缩放比例 | 水印相对于视频宽度的占比，范围 0.05～0.5，默认 0.2 |

**输出：** `watermark_001_xxx.mp4`。

---

### 8. 批量调整音量

统一调整文件夹内所有视频的音量大小。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 待处理视频来源 |
| 输出文件夹 | 结果保存路径 |
| 音量倍数 | `0.5` = 减半，`1.0` = 不变，`2.0` = 加倍，默认 1.0 |

**输出：** `volume_001_xxx.mp4`。

---

### 9. 批量格式转换

将文件夹内所有视频批量转换为指定格式。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 待处理视频来源 |
| 输出文件夹 | 结果保存路径 |
| 目标格式 | `mp4`（推荐）、`avi`、`mov`、`mkv` |

**输出：** 与原文件同名，扩展名替换为目标格式。

---

### 10. 批量提取帧

按固定时间间隔从视频中批量截图，输出为图片序列。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 待处理视频来源 |
| 输出文件夹 | 截图保存路径 |
| 提取间隔（秒）| 每隔 N 秒提取 1 帧，默认 5 秒 |

**输出：** 每个视频在输出目录下建立同名子文件夹，图片命名为 `frame_0001.jpg` 等。

---

## 第二行功能菜单（Row 2）

### 11. 填充音乐

为视频批量替换或混合背景音乐，支持**智能循环/裁剪**以适配视频时长。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 视频原片来源 |
| 音乐文件夹 | 背景音乐/视频来源（自动提取音频轨道）|
| 输出文件夹 | 结果保存路径 |
| 保留原生视频声音 | 勾选后将原音频与背景音乐混合；不勾选则完全替换音频 |

**核心逻辑：**
- 音乐自动循环拼接或裁剪，精确匹配视频总时长
- 支持从视频文件提取音轨作为背景音乐
- 同文件夹多个视频可以循环使用同一音乐池

**输出：** `music_001_xxx.mp4`。

---

### 12. 视频配音

将文案文本通过 **AI TTS（语音合成）** 生成配音，替换或混合到视频中，实现批量 AI 配音。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 原始视频来源（视频数量须与文案文件数一致）|
| 文案文件夹 | 每个视频对应一个 `.txt` 文本文件（按文件名排序配对）|
| 参考音频文件夹 | 用于克隆音色的参考音频（数量不足时自动用最后一个补齐）|
| 输出文件夹 | 结果保存路径 |
| 保留原音频（与配音混合）| 勾选后原始视频音频与 AI 配音混合；否则完全替换 |

**处理流程：**
1. 读取文案 `.txt`，按行拆分为多段话
2. 批量提交 MCP TTS 服务合成（参考音色克隆）
3. 多段合成音频拼接为完整配音轨
4. 将配音轨合并/替换回视频

**依赖：** MCP 服务（TTS）需在"设置"中配置地址。

**输出：** `dubbed_001_xxx.mp4`。

---

### 13. 视频翻译

对视频进行**完整 AI 翻译配音**：识别原声语言 → 翻译 → 重新配音 → 烧录字幕，全流程自动化完成。

| 参数 | 说明 |
|------|------|
| 视频文件夹 | 待翻译视频来源 |
| 输出文件夹 | 结果保存路径 |
| 目标语言 | `zh`（中文）/ `en`（英文）|

**11 步全自动处理流程：**

| 步骤 | 操作 | 技术 |
|------|------|------|
| 1 | 提取音频 | FFmpeg |
| 2 | 人声/背景分离 | Demucs（MCP 服务）|
| 3 | 截取参考音色（前 8 秒）| FFmpeg |
| 4 | 语音转文字转录 | Whisper（MCP 服务）|
| 5 | 文本翻译 | LLM API（GPT-4o 等）|
| 6 | 生成 SRT 字幕文件 | 同步输出到输出文件夹 |
| 7 | 批量 TTS 合成译文音频 | MCP TTS 服务（音色克隆）|
| 8 | TTS 音频时长对齐（变速/补静音）| FFmpeg atempo |
| 9 | 拼接音轨（含段间静音填充）| FFmpeg concat |
| 10 | 混合背景音乐（背景音 + 译配）| FFmpeg amix |
| 11 | 烧录字幕 + 合成最终视频 | FFmpeg ASS 字幕滤镜 |

**额外输出：** 每个视频同时输出一份 `.srt` 字幕文件（译文）。

**依赖：** 需在"设置"中配置 **MCP 服务地址**（Demucs + Whisper + TTS）和 **LLM API Key**。

**输出：** `xxx_translated_zh.mp4` + `xxx_translated_zh.srt`。

---

## 全局设置

通过右上角"**设置**"按钮打开，配置以下全局参数：

| 配置项 | 说明 |
|--------|------|
| MCP 服务地址 | AI 服务入口，默认 `http://localhost:8400/mcp`，视频配音和视频翻译共用 |
| LLM API Base | LLM 翻译服务基础 URL，默认 `https://api.openai.com/v1` |
| LLM API Key | LLM 访问密钥（视频翻译功能必填）|
| LLM Model | 翻译使用的模型，默认 `gpt-4o` |

---

## 支持的文件格式

| 类型 | 扩展名 |
|------|--------|
| 视频 | `.mp4` `.avi` `.mov` `.mkv` `.flv` `.wmv` `.webm` |
| 图片 | `.jpg` `.jpeg` `.png` `.bmp` `.tiff` |
| 音频 | `.mp3` `.wav` `.aac` `.flac` `.ogg` `.wma` `.m4a` |

---

## 功能依赖关系

```
基础视频处理（Row 1）
├── 左右分屏合并    → 仅 FFmpeg
├── 批量分割视频    → 仅 FFmpeg
├── 视频转场拼接    → 仅 FFmpeg
├── 画中画合成      → 仅 FFmpeg
├── 批量变速/倒放   → 仅 FFmpeg
├── 批量旋转/翻转   → 仅 FFmpeg
├── 批量添加水印    → 仅 FFmpeg
├── 批量调整音量    → 仅 FFmpeg
├── 批量格式转换    → 仅 FFmpeg
└── 批量提取帧      → 仅 FFmpeg

AI 增强处理（Row 2）
├── 填充音乐        → 仅 FFmpeg
├── 视频配音        → FFmpeg + MCP（TTS）
└── 视频翻译        → FFmpeg + MCP（Demucs + Whisper + TTS）+ LLM API
```
